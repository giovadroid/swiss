# Shared base layer, meant to be included from other manifests.
package_manager:
  linux:
    apt:
      update_index: true
      packages:
        - git
        - curl
        - build-essential

dependencies:
  cargo:
    cargo-binstall:
    ripgrep:
